[project]
name = "myagent"
version = "0.1.0"
description = "LLM-based coding agent (starter)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["ollama"]

[tool.uv]
# Nothing here yet.

